Instance Type - t2.micro
Ubuntu Version - 20.04

---- Install MongoDB ----

follow given link ---> https://linuxhint.com/install_mongodb_ubuntu_20_04/

--- configuration file setting --- 

vim /etc/mongodb.conf

bind_ip = 127.0.0.1
#port = 27017 ----> remove # and save

---- Install Node JS ----

curl -sL https://deb.nodesource.com/setup_14.x | sudo -E bash -
sudo apt-get install -y nodejs
node --version && npm --version

--- Clone code ---

cd /opt/
git clone giturl
npm install express
npm install
node server.js or npm start

---- nginx installation and configuration ----

sudo apt install nginx
cd /etc/nginx/sites-available/
vim default

-----------------------------------------------------------------------------

server {
        listen 80 default_server;
        listen [::]:80 default_server;

        root /var/www/html;

        index index.html index.htm index.nginx-debian.html;

        server_name _;

        location / {
                        proxy_pass http://publicIP:3000/hello_world;
        }
}

------------------------------------------------------------------------------

vim nodejs

------------------------------------------------------------------------------

server{
    listen 80;
    server_name node-server;
    location / {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_pass http://publicIP:3000/hello_world;
        proxy_http_version 1.1;
    }
}

-----------------------------------------------------------------------------

ln -s /etc/nginx/sites-available/nodejs /etc/nginx/sites-enabled/
rm /etc/nginx/sites-enabled/default
systemctl status nginx
systemctl restart nginx

copy your publicIP to check if application working 

[ NOTE : If you are making any changes inside path - /opt/NodeJs-API/app/controller , file - controller.js , then after changes please run node server.js or npm start. ]
